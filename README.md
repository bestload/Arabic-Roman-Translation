# Arabic-Roman-Translation

// Для использования кода, построчно копируй в терминал

``` bash
git clone https://github.com/bestload/Arabic-Roman-Translation.git
cd ./'Arabic-Roman-Translation'
g++ *.cpp -o 'art'
./art

```
